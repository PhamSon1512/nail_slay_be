#!/bin/sh
# Mirror main branch + all tags to winvuvn whenever pushing to origin
REMOTE="$1"
BRANCH=$(git symbolic-ref --short HEAD 2>/dev/null)

if [ "$REMOTE" = "origin" ] && [ "$BRANCH" = "main" ]; then
  echo "→ Mirroring to winvuvn..."
  git push winvuvn main
  git push winvuvn --tags
fi
